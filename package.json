{
  "name": "sih2-polar-station",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"python -m uvicorn backend.main:app --reload --port 8000\" \"live-server frontend --port=3000 --entry-file=frontend/dashboard.html\"",
    "start": "npm run dev"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "live-server": "^1.2.2"
  }
}